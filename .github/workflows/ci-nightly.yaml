name: CI - Nightly

on:
  workflow_dispatch:
  schedule:
    - cron: '30 3 * * *'

jobs:
  pr-checks:
    uses: ./.github/workflows/ci-pr.yaml

  cargo-hack-build:
    uses: ./.github/workflows/cargo-hack.yaml
    with:
      hack-subcommand: build
